export enum EMessageEmailStatusEnum {
  SENT_NOT_DELIVERED = 'SENT_NOT_DELIVERED',
  SENT_DELIVERED = 'SENT_DELIVERED',
  DELIVERED_READ = 'DELIVERED_READ'
}

export enum EMessageStatusTooltip {
  OPENED = 'Opened',
  DELIVERED = 'Delivered'
}
