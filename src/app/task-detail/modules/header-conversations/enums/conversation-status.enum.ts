export enum EConversationStatus {
  open = 'OPEN',
  reopen = 'REOPEN',
  resolved = 'RESOLVED',
  deleted = 'DELETED',
  schedule = 'SCHEDULE',
  draft = 'DRAFT'
}

export enum EConversationStatusTab {
  OPEN,
  RESOLVED,
  DELETED,
  DRAFT
}
